<h1>Welcome User {{user.firstName}} {{user.lastName}}</h1>
<br>


{{#if bookings}}
<h2>Rooms Booked:</h2>
     {{#each bookings}}
    <div>
        <img height="150px" width="150px"src="../img/{{this.roomPic}}" alt="{{this.roomName}}">
        <h4>{{this.roomName}}/{{this.roomLocation}}</h4>
        <p>${{this.roomPrice}}/Night</p>
    </div>
{{/each}}
{{else}}
<h2>You have no rooms booked!</h2>
<p><a href="/">Click Here</a> to book a room now!</p>
{{/if}}
